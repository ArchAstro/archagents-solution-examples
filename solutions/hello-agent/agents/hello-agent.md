# Hello Agent

A minimal starter agent: builtin knowledge search + memory, and it joins
threads when you @-mention it. No custom tools, scripts, or setup required.

Copy this solution and start adding your own tools, routines, and skills —
the `osv-vuln-checker` example shows the full pattern.
